from typing import Iterable, List
from common.models import NormalizedEvent, Alert

def detect(events: Iterable[NormalizedEvent]) -> List[Alert]:
    """TODO: 检测并映射 ATT&CK，输出 list[Alert]。"""
    return []
