# 直接复制给 AI

把整个 GitHub 项目提供给 AI 后，复制下面内容：

---

我正在参加一个 10 人课程项目，项目是“基于主机日志、主机行为、网络流量的恶意攻击行为溯源分析系统”。

我负责：【在这里写你的模块】

请先完整阅读：
1. 根目录 `AGENTS.md`
2. `docs/00_统一开发规范_AI执行版.md`
3. `docs/01_接口契约.md`
4. `common/models.py`
5. `common/enums.py`
6. `config/assets.json`
7. 我负责模块目录中的 README
8. 相关 `schemas/*.schema.json`

然后再开发。

强制要求：
- 只修改我负责的模块，除非我明确授权。
- 禁止私自修改 schemas、公共 models、公共 API、资产 ID 和前端公共接口类型。
- 跨模块数据必须使用公共模型。
- 如果现有接口不够用，不要直接改，先告诉我“需要改什么、为什么、会影响谁”。
- 开发完成后执行 `python scripts/validate_contracts.py` 和 `pytest -q`。
- 最终输出：修改文件、输入、输出、运行命令、测试方法、是否修改公共接口、已知限制。

现在先不要直接大改代码，先告诉我你理解的模块输入、输出和准备修改的文件。

---
