# 每日联调检查清单

每天至少一次。

## 每个成员回答

- [ ] 我的输入对象是什么？
- [ ] 我的输出对象是什么？
- [ ] 输出通过哪个 Schema？
- [ ] 我今天和哪个上下游模块实际连通过？
- [ ] 我的代码合并后 `main/dev` 是否还能运行？

## 全项目检查

```bash
python scripts/validate_contracts.py
pytest -q
```

后端：

```bash
uvicorn backend.main:app --reload
```

检查：
- [ ] `/api/health`
- [ ] `/api/events`
- [ ] `/api/alerts`
- [ ] `/api/attack-graph/task_demo_001`
- [ ] `/api/trace/task_demo_001`
- [ ] `/api/tasks/task_demo_001`

## 不允许“最后一天再联调”

Mock 数据存在的目的就是让下游不必等待上游。
